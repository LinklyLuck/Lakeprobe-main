# LakeProbe — Dependencies

# Core
pydantic>=2.0
duckdb>=0.9.0
scikit-learn>=1.3.0

# Knowledge base (optional — system works without via extended dict fallback)
nltk>=3.8.0

# Token measurement
tiktoken>=0.5.0

# API
fastapi>=0.100.0
uvicorn>=0.20.0

# UI (optional)
streamlit>=1.30.0

# LLM (optional — system works without via fallback parser)
openai>=1.0.0

# Alternative executor (optional)
polars>=0.20.0
pandas>=2.0.0
