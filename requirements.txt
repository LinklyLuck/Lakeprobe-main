# Python >= 3.10 required

# ── Core Pipeline ──
pydantic>=2.0
numpy>=1.24
openai>=1.0                # LLM API (GPT / DeepSeek / Gemini via OpenAI-compatible)

# ── Data Processing ──
duckdb>=0.9                # SQL execution engine
pandas>=2.0                # DataFrame operations (profiler fallback)
polars>=0.19               # Fast DataFrame (executor alternative)

# ── Embedding & Retrieval ──
sentence-transformers>=2.2 # Local embedding model (all-MiniLM-L6-v2)
scikit-learn>=1.3          # TF-IDF vectorizer + TruncatedSVD

# ── Knowledge Base ──
nltk>=3.8                  # WordNet synonym discovery (offline)

# ── Token Tracking ──
tiktoken>=0.5              # OpenAI tokenizer for precise token counting

# ── Web UI ──
streamlit>=1.28            # Demo interface
fastapi>=0.100             # REST API
uvicorn>=0.23              # ASGI server

# ── Post-install ──
# Run once after install:
#   python -c "import nltk; nltk.download('wordnet'); nltk.download('omw-1.4')"
